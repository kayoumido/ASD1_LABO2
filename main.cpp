#include <iostream>
#include <algorithm>
#include "pieces.h"

using namespace std;

int main() {

    // placePiece(PIECES);

    placePiece(usedPieces, availablePieces, piecesOrientations);
    
    return 0;
}
